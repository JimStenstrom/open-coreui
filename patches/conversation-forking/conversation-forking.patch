diff --git a/svelte-frontend/src/lib/components/chat/Messages.svelte b/svelte-frontend/src/lib/components/chat/Messages.svelte
index ab9f722..d86a798 100755
--- a/svelte-frontend/src/lib/components/chat/Messages.svelte
+++ b/svelte-frontend/src/lib/components/chat/Messages.svelte
@@ -7,14 +7,16 @@
 		user as _user,
 		mobile,
 		currentChatPage,
-		temporaryChatEnabled
+		temporaryChatEnabled,
+		chatTitle
 	} from '$lib/stores';
 	import { tick, getContext, onMount, createEventDispatcher } from 'svelte';
 	const dispatch = createEventDispatcher();
 
+	import { goto } from '$app/navigation';
 	import { toast } from 'svelte-sonner';
-	import { getChatList, updateChatById } from '$lib/apis/chats';
-	import { copyToClipboard, extractCurlyBraceWords } from '$lib/utils';
+	import { getChatList, updateChatById, createNewChat } from '$lib/apis/chats';
+	import { copyToClipboard, extractCurlyBraceWords, createMessagesList } from '$lib/utils';
 
 	import Message from './Messages/Message.svelte';
 	import Loader from '../common/Loader.svelte';
@@ -397,6 +399,105 @@
 			}, 100);
 		}
 	};
+
+	const forkConversation = async (messageId: string) => {
+		if ($temporaryChatEnabled) {
+			toast.error($i18n.t('Cannot fork a temporary chat'));
+			return;
+		}
+
+		try {
+			// Build the forked history - include all messages up to and including the fork point
+			const forkedMessages = {};
+			const message = history.messages[messageId];
+
+			if (!message) {
+				toast.error($i18n.t('Message not found'));
+				return;
+			}
+
+			// Traverse from the fork point back to the root to get all ancestor messages
+			let currentMessage = message;
+			const messageIds: string[] = [];
+
+			while (currentMessage) {
+				messageIds.unshift(currentMessage.id);
+				currentMessage = currentMessage.parentId ? history.messages[currentMessage.parentId] : null;
+			}
+
+			// Create new message IDs for the forked conversation
+			const oldToNewIdMap: Record<string, string> = {};
+			messageIds.forEach(id => {
+				oldToNewIdMap[id] = uuidv4();
+			});
+
+			// Build the forked messages with new IDs
+			for (const oldId of messageIds) {
+				const originalMsg = history.messages[oldId];
+				const newId = oldToNewIdMap[oldId];
+				const newParentId = originalMsg.parentId ? oldToNewIdMap[originalMsg.parentId] : null;
+
+				// For the last message (fork point), set childrenIds to empty
+				// For other messages, map the child that's in our chain
+				let newChildrenIds: string[] = [];
+				if (oldId !== messageId) {
+					// Find which child is in our chain (guard against undefined childrenIds)
+					const childInChain = (originalMsg.childrenIds ?? []).find(childId => messageIds.includes(childId));
+					if (childInChain) {
+						newChildrenIds = [oldToNewIdMap[childInChain]];
+					}
+				}
+
+				forkedMessages[newId] = {
+					...JSON.parse(JSON.stringify(originalMsg)),
+					id: newId,
+					parentId: newParentId,
+					childrenIds: newChildrenIds
+				};
+			}
+
+			const newCurrentId = oldToNewIdMap[messageId];
+			const forkedHistory = {
+				messages: forkedMessages,
+				currentId: newCurrentId
+			};
+
+			// Generate title for the forked chat
+			const firstUserMessage = Object.values(forkedMessages).find(
+				(m: any) => m.role === 'user'
+			) as any;
+			const forkTitle = firstUserMessage?.content
+				? `${$i18n.t('Fork')}: ${firstUserMessage.content.slice(0, 30)}${firstUserMessage.content.length > 30 ? '...' : ''}`
+				: `${$i18n.t('Fork')}: ${$chatTitle || $i18n.t('New Chat')}`;
+
+			// Create the new chat
+			const newChat = await createNewChat(
+				localStorage.token,
+				{
+					title: forkTitle,
+					models: selectedModels,
+					history: forkedHistory,
+					messages: createMessagesList(forkedHistory, forkedHistory.currentId),
+					timestamp: Date.now()
+				},
+				null
+			);
+
+			if (newChat) {
+				toast.success($i18n.t('Conversation forked successfully'));
+
+				// Update the chat list
+				currentChatPage.set(1);
+				await chats.set(await getChatList(localStorage.token, $currentChatPage));
+
+				// Navigate to the new chat
+				await goto(`/c/${newChat.id}`);
+			}
+		} catch (error) {
+			console.error('Error forking conversation:', error);
+			toast.error($i18n.t('Failed to fork conversation'));
+		}
+	};
 </script>
 
 <div class={className}>
@@ -447,6 +548,7 @@
 								{mergeResponses}
 								{addMessages}
 								{triggerScroll}
+								{forkConversation}
 								{readOnly}
 								{editCodeBlock}
 								{topPadding}
diff --git a/svelte-frontend/src/lib/components/chat/Messages/Message.svelte b/svelte-frontend/src/lib/components/chat/Messages/Message.svelte
index d2c2846..dca56ec 100755
--- a/svelte-frontend/src/lib/components/chat/Messages/Message.svelte
+++ b/svelte-frontend/src/lib/components/chat/Messages/Message.svelte
@@ -40,6 +40,7 @@
 
 	export let addMessages;
 	export let triggerScroll;
+	export let forkConversation: Function = () => {};
 	export let readOnly = false;
 	export let editCodeBlock = true;
 	export let topPadding = false;
@@ -68,6 +69,7 @@
 				{showNextMessage}
 				{editMessage}
 				{deleteMessage}
+				{forkConversation}
 				{readOnly}
 				{editCodeBlock}
 				{topPadding}
@@ -93,6 +95,7 @@
 				{deleteMessage}
 				{continueResponse}
 				{regenerateResponse}
+				{forkConversation}
 				{addMessages}
 				{readOnly}
 				{editCodeBlock}
diff --git a/svelte-frontend/src/lib/components/chat/Messages/ResponseMessage.svelte b/svelte-frontend/src/lib/components/chat/Messages/ResponseMessage.svelte
index 945b576..d6f1602 100755
--- a/svelte-frontend/src/lib/components/chat/Messages/ResponseMessage.svelte
+++ b/svelte-frontend/src/lib/components/chat/Messages/ResponseMessage.svelte
@@ -135,6 +135,7 @@
 	export let submitMessage: Function;
 	export let continueResponse: Function;
 	export let regenerateResponse: Function;
+	export let forkConversation: Function = () => {};
 
 	export let addMessages: Function;
 
@@ -984,6 +985,36 @@
 									</button>
 								</Tooltip>
 
+								{#if !readOnly}
+									<Tooltip content={$i18n.t('Fork')} placement="bottom">
+										<button
+											aria-label={$i18n.t('Fork')}
+											class="{isLastMessage || ($settings?.highContrastMode ?? false)
+												? 'visible'
+												: 'invisible group-hover:visible'} p-1.5 hover:bg-black/5 dark:hover:bg-white/5 rounded-lg dark:hover:text-white hover:text-black transition"
+											on:click={() => {
+												forkConversation(message.id);
+											}}
+										>
+											<svg
+												xmlns="http://www.w3.org/2000/svg"
+												fill="none"
+												aria-hidden="true"
+												viewBox="0 0 24 24"
+												stroke-width="2.3"
+												stroke="currentColor"
+												class="w-4 h-4"
+											>
+												<path
+													stroke-linecap="round"
+													stroke-linejoin="round"
+													d="M7.217 10.907a2.25 2.25 0 1 0 0 2.186m0-2.186c.18.324.283.696.283 1.093s-.103.77-.283 1.093m0-2.186 9.566-5.314m-9.566 7.5 9.566 5.314m0 0a2.25 2.25 0 1 0 3.935 2.186 2.25 2.25 0 0 0-3.935-2.186Zm0-12.814a2.25 2.25 0 1 0 3.933-2.185 2.25 2.25 0 0 0-3.933 2.185Z"
+												/>
+											</svg>
+										</button>
+									</Tooltip>
+								{/if}
+
 								{#if $user?.role === 'admin' || ($user?.permissions?.chat?.tts ?? true)}
 									<Tooltip content={$i18n.t('Read Aloud')} placement="bottom">
 										<button
diff --git a/svelte-frontend/src/lib/components/chat/Messages/UserMessage.svelte b/svelte-frontend/src/lib/components/chat/Messages/UserMessage.svelte
index d58b9da..063a155 100755
--- a/svelte-frontend/src/lib/components/chat/Messages/UserMessage.svelte
+++ b/svelte-frontend/src/lib/components/chat/Messages/UserMessage.svelte
@@ -35,6 +35,7 @@
 
 	export let editMessage: Function;
 	export let deleteMessage: Function;
+	export let forkConversation: Function = () => {};
 
 	export let isFirstMessage: boolean;
 	export let readOnly: boolean;
@@ -515,6 +516,34 @@
 						</Tooltip>
 					{/if}
 
+					{#if !readOnly}
+						<Tooltip content={$i18n.t('Fork')} placement="bottom">
+							<button
+								class="{($settings?.highContrastMode ?? false)
+									? ''
+									: 'invisible group-hover:visible'} p-1.5 hover:bg-black/5 dark:hover:bg-white/5 rounded-lg dark:hover:text-white hover:text-black transition"
+								on:click={() => {
+									forkConversation(message.id);
+								}}
+							>
+								<svg
+									xmlns="http://www.w3.org/2000/svg"
+									fill="none"
+									viewBox="0 0 24 24"
+									stroke-width="2.3"
+									stroke="currentColor"
+									class="w-4 h-4"
+								>
+									<path
+										stroke-linecap="round"
+										stroke-linejoin="round"
+										d="M7.217 10.907a2.25 2.25 0 1 0 0 2.186m0-2.186c.18.324.283.696.283 1.093s-.103.77-.283 1.093m0-2.186 9.566-5.314m-9.566 7.5 9.566 5.314m0 0a2.25 2.25 0 1 0 3.935 2.186 2.25 2.25 0 0 0-3.935-2.186Zm0-12.814a2.25 2.25 0 1 0 3.933-2.185 2.25 2.25 0 0 0-3.933 2.185Z"
+									/>
+								</svg>
+							</button>
+						</Tooltip>
+					{/if}
+
 					{#if $_user?.role === 'admin' || ($_user?.permissions?.chat?.delete_message ?? false)}
 						{#if !readOnly && (!isFirstMessage || siblings.length > 1)}
 							<Tooltip content={$i18n.t('Delete')} placement="bottom">
